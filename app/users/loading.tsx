// Import the LoadingModal component for displaying a loading state
import LoadingModal from "../components/LoadingModal";

// Create the Loading component
const Loading = () => {
  return <LoadingModal />; // Use the LoadingModal for /users
};

export default Loading; // Export the Loading component
